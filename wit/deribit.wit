interface deribit-positions-handler {
  use jabratech:common/types.{response};
  use types.{deribit-positions-response};

  get:  func(currency: string) -> result<deribit-positions-response, response>;
  
}